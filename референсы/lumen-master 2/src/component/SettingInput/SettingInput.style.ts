import CreateStyles from 'Util/CreateStyles';

export const styles = CreateStyles({
  overlay: {
    gap: 16,
  },
  overlayTitle: {
    fontSize: 20,
    fontWeight: '600',
  },
  overlayInput: {
  },
  overlayButton: {
  },
});