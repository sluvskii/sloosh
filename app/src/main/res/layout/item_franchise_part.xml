<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:background="?attr/selectableItemBackground"
    android:padding="12dp">

    <TextView
        android:id="@+id/part_number"
        android:layout_width="24dp"
        android:layout_height="wrap_content"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/part_name"
        app:layout_constraintBottom_toBottomOf="@id/part_name"
        tools:text="1" />

    <TextView
        android:id="@+id/part_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:textColor="?attr/colorPrimary"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toStartOf="@id/part_rating"
        app:layout_constraintStart_toEndOf="@id/part_number"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Название части фильма" />

    <TextView
        android:id="@+id/part_info"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="14sp"
        app:layout_constraintEnd_toStartOf="@id/part_rating"
        app:layout_constraintStart_toStartOf="@id/part_name"
        app:layout_constraintTop_toBottomOf="@id/part_name"
        tools:text="2023" />

    <TextView
        android:id="@+id/part_rating"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:gravity="end"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/part_name"
        app:layout_constraintBottom_toBottomOf="@id/part_info"
        tools:text="7.5" />

</androidx.constraintlayout.widget.ConstraintLayout>
