import CreateStyles from 'Util/CreateStyles';

export const styles = CreateStyles({
  overlay: {
    gap: 16,
    maxHeight: 288,
    width: 250,
  },
  overlayTitle: {
    fontSize: 20,
    fontWeight: '600',
  },
  overlayInput: {
  },
  overlayButton: {
  },
});